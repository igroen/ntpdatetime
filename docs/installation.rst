============
Installation
============

At the command line::

    $ easy_install ntpdatetime

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ntpdatetime
    $ pip install ntpdatetime
